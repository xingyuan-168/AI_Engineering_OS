schema_version: '1.2'
gate: G0
documents: [artifact:project-master, artifact:scope]
required_records: [routing-decision]
git: {source_commit_required: true, push_required: true, enforce_allowed_paths: true}
