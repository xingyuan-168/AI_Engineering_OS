schema_version: '1.2'
gate: G2
documents: [docs/ENVIRONMENT.md]
required_artifacts:
  - .codex-os/environment.yaml
  - .dockerignore
  - compose.yaml
  - docker/README.md
required_checks: [environment-contract]
git: {source_commit_required: true, push_required: true, enforce_allowed_paths: true}
