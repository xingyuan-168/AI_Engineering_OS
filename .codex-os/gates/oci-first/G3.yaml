schema_version: '1.2'
gate: G3
required_artifact_types: [environment-prepare-evidence, environment-rebuild-report]
required_checks:
  - host-cleanliness
  - compose-build
  - container-recreate
  - storage-persistence
  - environment-smoke
git: {source_commit_required: true, push_required: true, enforce_allowed_paths: true}
