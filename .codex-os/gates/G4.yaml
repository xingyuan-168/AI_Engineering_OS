schema_version: '1.2'
gate: G4
required_artifact_types: [changelog, release-manifest, rollback, sbom, checksums, memory]
required_checks: [release-manifest, sbom, checksums, rollback]
required_reviews: [release]
git: {source_commit_required: true, push_required: true, enforce_allowed_paths: true}
